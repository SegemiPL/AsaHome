// Phase 3 stub — placeholder component

export interface CharacterPortraitProps {
  characterId?: string;
  state?: string;
  className?: string;
}

/**
 * Phase 3 placeholder.
 * Will render a character portrait with transitions and fallback.
 */
export function CharacterPortrait(_props: CharacterPortraitProps): null {
  // Not implemented — Phase 3 placeholder
  return null;
}
