[package]
name = "asahome-desktop"
version = "0.1.0"
description = "AsaHome Desktop Pet — Tauri 2 (Phase 3 stub)"
authors = ["SegemiPL"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
