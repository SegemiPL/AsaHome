---
# 文章标题（必填）
title: "文章标题"

# URL 中的文章标识（必填），例如 /blog/my-new-post
# 建议：小写字母、数字、短横线，不要使用空格或中文
slug: "my-new-post"

# 发布日期（必填），格式：YYYY-MM-DD
date: "2026-06-25"

# 文章摘要（必填），显示在 Blog 列表页，建议 1-2 句话
summary: "这里是文章摘要，概括文章的核心内容。"

# 封面图片路径（可选），例如 "/images/cover.png"
# 图片文件放在 apps/web/public/ 下
cover: null

# 是否为草稿（必填）
# draft: true  不会在站点上显示
# draft: false 发布后可见
draft: true
---

## 引言

在这里写文章的引言部分。简单介绍背景、问题或动机。

## 正文

这是正文段落。你可以使用常见的 Markdown 语法：

- **粗体文字**
- *斜体文字*
- `行内代码`
- [链接文本](https://example.com)
- 无序列表项

### 小标题

可以用 `##` 和 `###` 来划分章节。

```
代码块示例（当前渲染器按普通文本显示）
```

> 提示：当前站点使用的 Markdown 渲染器支持：标题、粗体、斜体、行内代码、链接、图片、分隔线、无序列表。

## 后续

总结全文，或者留下下一步计划、参考资料。

---

**使用方法**：复制本文件，重命名为 `你的文章 slug.md`，把 `slug` 和 `date` 改成实际值，将 `draft` 改为 `false`，然后替换上面的示例内容即可。
